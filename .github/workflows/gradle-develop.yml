name: Fineract ibanera Build

on:
  push:
    branches:
      - frank/dev

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - uses: kamiazya/setup-graphviz@v1

      - name: Set up JDK 12
        uses: actions/setup-java@v1
        with:
          java-version: 12

      - name: Login to Github Packages Docker
        uses: azure/docker-login@v1
        with:
          login-server: 'docker.pkg.github.com'
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build with Gradle
        run: ./gradlew clean build docker dockerPush -x test
        env:
          GPR_USERNAME: ${{ secrets.GPR_USERNAME }}
          GPR_PASSWORD: ${{ secrets.GPR_PASSWORD }}

#      - name: Deploy to sandbox
#        run: |
#          echo "$IBANERA_SSH_KEY" > key.pem
#          chmod 400 key.pem
#
#          echo "Restart docker container...!!!"
#          ssh -i key.pem -o StrictHostKeyChecking=no $IBANERA_URL '/home/ubuntu/scripts/deploy-fineract.sh'
#        env:
#          IBANERA_SSH_KEY: ${{secrets.IBANERA_SSH_KEY}}
#          IBANERA_URL: ${{secrets.IBANERA_URL}}
